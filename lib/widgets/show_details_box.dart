import 'package:flutter/material.dart';

class ShowDetailsBox extends StatelessWidget {
  const ShowDetailsBox({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: double.infinity,
      decoration: BoxDecoration(

      ),
    );
  }
}
